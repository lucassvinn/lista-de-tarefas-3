#include <stdio.h>

int main()
{
    char letras[3] = {'A', 'B', 'C'};
    
    for (letras[0]; letras[0]<=letras[2]; letras[0]++) {
        printf("%c\n", letras[0]);
    }
    // Apenas um teste de código...estudando o for de outras maneiras
    
    return 0;
}
